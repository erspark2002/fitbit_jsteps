#_Running

NOTE: fitbit app MUST be set to "client" type in order to use response_type=token.

ngrok MUST be used as localhost is not allowed.

----------------
#_Running

$ live-server

-------------------